{
  "name": "buildid",
  "version": "0.1.0",
  "private": true,
  "description": "BuildID — remote-control link between a desktop host and a web viewer.",
  "workspaces": [
    "server",
    "desktop"
  ],
  "scripts": {
    "server": "npm --workspace server start",
    "desktop": "npm --workspace desktop start",
    "install:all": "npm install"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
